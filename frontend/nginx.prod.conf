server {
  listen 80;

  # SPA fallback — the LB URL map handles API routing before requests reach nginx
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}
