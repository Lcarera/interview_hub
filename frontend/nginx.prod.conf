server {
  listen 80;

  # SPA fallback — Cloudflare Worker handles API routing before requests reach nginx
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}
